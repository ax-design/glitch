# PNG Glitch Art 策略手册

> 所有策略均基于 PNG 编码流程：Raw Data → Filtered Data → Compressed Data → Formatted PNG。
> 前提：直接改写二进制文件会导致 CRC32 校验失败，查看器拒绝显示。所有操作必须在修改数据后重新计算并写回 CRC32，或使用能自动处理 CRC 的工具（如 PNGlitch）。

---

## 一、Filtered Data 层策略

最具 PNG 特色的 glitch 层。PNG 在压缩前对每条 scanline 应用 filter，将像素转为差值形式。在这一层修改数据，效果由所用 filter 的传播方向决定。

### 1.1 Sub Filter 下的破坏

**传播方向**：水平向右

Sub 算法存储当前像素与左侧像素的差值。改写一个字节，从该位置起直到行尾所有像素都会受到影响。

- **替换字节**：产生向右蔓延的色彩雪崩。
- **删除字节**：造成 scanline 整体右移，产生位移错位效果。
- **转置 chunk**：大块内容水平方向打乱。

### 1.2 Up Filter 下的破坏

**传播方向**：垂直向下

Up 算法存储当前像素与正上方像素的差值。改写一个字节，从该行往下所有行都会受到影响。

- **替换字节**：产生向下蔓延的垂直条纹。
- **删除字节**：下方所有行整体上移一格，产生垂直位移。
- **转置 chunk**：垂直方向大块内容打乱。

### 1.3 Average Filter 下的破坏

**传播方向**：右下对角线

Average 算法取左侧和上方像素的均值作为基准。改写一个字节，影响沿右下方向扩散，强度渐弱，形成喷洒渐变。

- **替换字节**：产生从破坏点发出的对角渐变喷洒，有时二次传播。
- **对算法本身施加 bitmask**：输出结果被遮罩，颜色向下方"流淌"。
- **修改 filter 实现**（如先 Up 再 mask 再 Average）：每行第一像素使用变体算法，其余像素正常，产生受控的周期性效果。

### 1.4 Paeth Filter 下的破坏

**传播方向**：右侧 + 下方（最广）

Paeth 算法在左、上、左上三个像素中启发式选一个作为基准。改写单个字节即可影响其右侧和下方的大块区域。

- **替换字节**：产生大范围块状扰动，同时保留原图结构的隐约轮廓。
- **对算法输出施加 bitmask**：产生与 Average mask 风格迥异的狂野色彩效果。
- **修改 filter 实现**（如先 Up 再 mask 再 Paeth）：每行第一像素使用变体，迷幻感强烈，原图结构仍隐约可见。
- **使用有缺陷的 Paeth 实现**（如用无符号整数替代有符号整数）：效果介于 Paeth 与 Average 之间，产生意外的混合风格。

### 1.5 None Filter 下的破坏

不应用任何 filter，直接存储原始 RGB 值。

- **替换字节**：仅影响单个像素，改变其颜色，影响范围极度局部。
- **删除字节**：造成整行乃至后续行内容整体位移，效果比替换更有趣。

### 1.6 混合 Filter（每行随机）

使用混合 filter（优化 PNG 的默认状态）时，各 filter 的效果在同一张图上同时显现，体现出原始 PNG 编码时各 filter 的分布情况。

- **替换字节**：结果由当时所在 scanline 使用的 filter 决定，体现出 filter 分布的边界。
- **强制每行随机选择 filter type**：每行之间 filter 跳变，产生各段风格不一的混合效果。设定一定概率（如 5%）触发切换，效果比完全随机更可控。

---

## 二、Raw Data 层策略

直接操作过滤前的原始像素数据。效果等同于 None filter 下的 Filtered Data 操作，影响范围局部。不具备 PNG 特有的传播性，但仍是有效的 glitch 手段。

- **替换字节**：单个像素颜色发生变化，影响范围仅限该像素。
- **删除字节**：整行像素向左移位，产生滑动错位效果。

---

## 三、Compressed Data 层策略（IDAT 内部）

直接改写 IDAT chunk 中经 Deflate 压缩后的数据。由于 Deflate 的字典依赖特性，改写位置对破坏范围的影响极大。

- **改写靠后位置的字节**：破坏范围相对有限，图像末尾出现彩虹噪点或色带。
- **改写中部字节**：大范围的暴风雪噪点，效果与 filter 层的 glitch 视觉上相似，但只改了一个字节。
- **改写靠前位置的字节**：几乎摧毁整张图像，原图结构难以辨认，偶有 filter 残影。

---

## 四、Filter Type Byte 策略（Graft 技术）

每条 scanline 的第一个字节是 filter type 声明（0~4）。在不修改任何像素数据的前提下，只改写这一个字节，解码器就会用错误的 filter 反算像素值，产生算法性色彩扭曲。

- **将所有 scanline 的 filter type 改为同一值**：整张图用同一种错误 filter 解码，效果整齐而系统。
- **将所有 scanline 的 filter type 随机化**：每行用不同的错误 filter 解码，产生多风格混叠效果。
- **仅修改部分 scanline 的 filter type**：图像中产生局部的算法性破坏，其余部分正常显示。

---

## 五、自定义错误 Filter 编码策略

将图像编码时使用的 filter 算法替换为非标准实现，解码器仍按标准 filter 反算，从而产生系统性算法偏差。

- **反向像素引用**（从右向左而非从左向右）：产生向左传播的镜像扭曲。
- **偏移引用位置**（引用左移 N 位的像素而非相邻像素）：产生周期性条纹或波纹。
- **组合多层 filter**（先 Paeth 编码再以 Up 标记 type）：解码器解出与原始数据双重偏差的结果。
- **有缺陷的类型实现**（如 Paeth 中用无符号整数代替有符号整数）：混合两种 filter 的视觉特征，产生意外风格。

---

## 六、结构变量（与上述策略叠加使用）

以下因素不是独立策略，而是会放大或改变上述所有策略效果的变量。

### 6.1 Interlace（Adam7 算法）

启用 interlace 的 PNG 按 Adam7 算法将图像分为 7 个 pass 存储，每个 pass 覆盖 8×8 像素网格中的不同位置。在 interlaced PNG 上应用任何 filter 层策略，glitch 效果会沿 pass 边界呈现交织结构，角度随 filter 种类而变化。

### 6.2 Alpha 透明通道

含透明像素的图像被 glitch 时，透明度参与色彩混合，本身成为视觉效果的一部分。Average filter 尤其倾向于将透明像素以渐变方式扩散融合；纯色区域在 Up filter 下的行为与复杂图像区域明显不同。

### 6.3 图像内容类型

纯色大块区域的压缩特性与复杂图像区域不同，同样的破坏操作在两种区域上效果差异显著。含有大片纯色（如卡通、图标）的图像是研究各 filter 差异的理想测试对象。

---

## 七、操作方法分类

无论在哪一层操作，具体的数据修改方式可以分为以下三类，效果各异：

| 方法 | 说明 | 典型效果 |
|------|------|----------|
| 替换（Replace） | 将目标字节随机改写为新值 | 色彩扭曲，传播方向由 filter 决定 |
| 删除（Defect）  | 从数据流中移除目标字节   | 后续内容整体位移，产生错位 |

---

## 参考来源

- UCNV, *The Art of PNG Glitch*, https://ucnv.github.io/pnglitch/
- glitch.art, *PNG*, https://glitch.art/png
- Travis Mick, *Adventures in image glitching*, https://lo.calho.st/posts/image-glitching/
- W3C, *PNG Specification: Filter Algorithms*, https://www.w3.org/TR/PNG-Filters.html